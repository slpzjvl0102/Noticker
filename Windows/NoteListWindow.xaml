<Window x:Class="Noticker.Windows.NoteListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="스티커 메모" Width="320" Height="500"
        MinWidth="240" MinHeight="300"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False"
        ResizeMode="CanResize">
    <DockPanel x:Name="RootPanel">
        <!-- 검색 -->
        <Border x:Name="SearchBorder" DockPanel.Dock="Top" Padding="8,6"
                BorderThickness="0,0,0,1">
            <Grid>
                <TextBox x:Name="SearchBox" TextChanged="SearchBox_TextChanged"
                         Padding="4,4" BorderThickness="1"/>
                <TextBlock x:Name="SearchPlaceholder" Text="검색..."
                           Padding="6,4" IsHitTestVisible="False"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=SearchBox, Path=Text.Length}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>

        <!-- 빈 상태 메시지 -->
        <TextBlock x:Name="EmptyLabel"
                   Visibility="Collapsed"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Margin="24,0"
                   DockPanel.Dock="Bottom"/>

        <!-- 노트 목록 -->
        <ListView x:Name="NoteList"
                  SelectionChanged="NoteList_SelectionChanged"
                  BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border x:Name="ItemBorder" Background="Transparent">
                                    <ContentPresenter/>
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal"/>
                                            <VisualState x:Name="MouseOver">
                                                <Storyboard>
                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ItemBorder" Storyboard.TargetProperty="Background">
                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{x:Static SystemColors.HighlightBrush}"/>
                                                    </ObjectAnimationUsingKeyFrames>
                                                </Storyboard>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border x:Name="RowBorder" Padding="12,8" BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <!-- 본문 상단 3줄 (주 내용) -->
                                <TextBlock x:Name="BodyPreview"
                                           Text="{Binding Preview}"
                                           TextTrimming="CharacterEllipsis"
                                           MaxHeight="54"
                                           TextWrapping="Wrap"
                                           FontSize="12"/>
                                <!-- 제목 + 날짜 + 숨김 배지 -->
                                <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                    <TextBlock x:Name="TitleLabel"
                                               Text="{Binding Title}"
                                               FontSize="10"
                                               Visibility="{Binding TitleVisibility}"/>
                                    <TextBlock x:Name="DateLabel"
                                               Text="{Binding DateLabel}"
                                               FontSize="10"/>
                                    <TextBlock Text=" · 숨김" FontSize="10"
                                               Visibility="{Binding IsHiddenBadge}"/>
                                </StackPanel>
                            </StackPanel>
                            <Button Grid.Column="1" Content="삭제"
                                    Tag="{Binding Id}"
                                    Click="DeleteButton_Click"
                                    Padding="6,2" Margin="8,0,0,0"
                                    VerticalAlignment="Center"
                                    Cursor="Hand"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </DockPanel>
</Window>
